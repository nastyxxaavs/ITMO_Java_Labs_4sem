CREATE TABLE users (
                      id UUID NOT NULL PRIMARY KEY,
                      user_name varchar(250),
                      full_name varchar(250),
                      email varchar(250),
                      password varchar(250)
);